<div class="box">
    <div class="container">

        <h1>{{course.title}}</h1>
        <hr />
        <p><img src="{{course.image}}"/></p>
        <i>{{course.description}}</i><br/>
        <label>{{course.level}} | {{course.hours}}</label><br/>
        <strong>R$ {{course.price}}</strong><br/>
        <hr />
        <a class="btn btn-success" href="/courses" role="button">Back</a>
        <hr />
        <h3>Enrolled Students</h3>
    </div>
    
</div>